# CCTV API config for production builds
# Point this to your own backend/proxy endpoint (not direct third-party API unless CORS allows your origin)
VITE_CCTV_API_URL=

# Optional: override API key in production
VITE_CCTV_API_KEY=
