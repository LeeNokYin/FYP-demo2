## Archive Notes

- `MapOverlays.jsx` was archived during feature consolidation.
- Active map UI now renders directly from `MapView.jsx`.
- Legacy audio overlay split components were removed from `src/components/features/audio`.
